overwrite: true
schema: 'http://localhost:3005/graphql'
documents: null
generates:
  ./shared/graphql/types.d.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      typesPrefix: GQL
